[training]
n_episodes = 10000
rollout_steps = 2048
n_parallel_envs = 1

[ppo]
learning_rate = 0.0003
gamma = 0.99
gae_lambda = 0.95
clip_epsilon = 0.2
entropy_coef = 0.01
value_coef = 0.5
max_grad_norm = 0.5
n_epochs = 10
batch_size = 64

[network]
hidden_sizes = [256, 128]

[logging]
log_interval = 10
save_interval = 100
checkpoint_dir = "checkpoints"
