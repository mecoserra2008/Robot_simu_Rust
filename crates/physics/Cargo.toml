[package]
name = "physics"
version = "0.1.0"
edition = "2021"

[dependencies]
rapier3d = { version = "0.17", features = ["parallel", "simd-stable"] }
nalgebra = "0.32"
serde = { version = "1.0", features = ["derive"] }
