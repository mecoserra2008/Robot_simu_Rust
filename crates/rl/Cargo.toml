[package]
name = "rl"
version = "0.1.0"
edition = "2021"

[dependencies]
nalgebra = "0.32"
ndarray = { version = "0.15", features = ["rayon"] }
rand = "0.8"
rand_distr = "0.4"
rayon = "1.8"
serde = { version = "1.0", features = ["derive"] }
